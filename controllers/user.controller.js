const { User } = require('../models/user.model');

// Create user (POST)

// Login (POST)

// Update user (PATCH)

// Delete user (DELETE)

// Get user's orders (GET)

// Get an user's single order (GET)

